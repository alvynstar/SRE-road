apiVersion: v1
kind: Service
metadata:
  name: sre-lab-app
  namespace: default
  labels:
    app: sre-lab-app
spec:
  type: ClusterIP
  selector:
    app: sre-lab-app
  ports:
  - port: 80
    targetPort: 5001
    protocol: TCP
    name: http
