apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: sre-lab-app
  namespace: sre-lab
  labels:
    release: kube-prometheus-stack
spec:
  selector:
    matchLabels:
      app: sre-lab-app
  endpoints:
  - port: http
    interval: 30s
